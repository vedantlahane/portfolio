{
    "buildCommand": "rm -rf node_modules .vite dist package-lock.json && npm install --force && npm run build",

  "outputDirectory": "dist",
  "framework": "vite",
    "rewrites": [
      {
        "source": "/(.*)",
        "destination": "/index.html"
      }
    ]
  }